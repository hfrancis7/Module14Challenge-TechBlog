<section class="post-card">
    <div class="post-header">
        <h2>{{title}}</h2>
        <p>Posted by {{user.name}} on {{format_date post_dateCreated}}</p>
    </div>
    <div class="post-description-style">
        <p>{{post_description}}</p>
    </div>
</section>

{{#each comments as |comment|}}
<section class="post-card">
    <div class="post-header">
        <p>{{comment.user.name}} ({{format_date comment.comment_dateCreated}})</p>
    </div>
    <div class="post-description-style">
        <p>{{comment.comment_description}}</p>
    </div>
</section>
{{/each }}

<section class="post-card">
    <div class="comment-header">
        <h2>Add Comment</h2>
    </div>
    <form class="createComment-form">
        <div class="flex-comment-input">
                <input class="form-input" type="text" id="create-comment-input">
        </div>
        <div class="flex-center-comment-btn">
            <button>Comment</button>
        </div>
    </form>
</section>

<script src="/js/comment.js"></script>